# BharatBeat frontend env template. Copy to .env for local dev.
# VITE_* are read via import.meta.env and baked in at build time.
# Both default to localhost:8000 if unset (see src/lib/api.ts).
VITE_API_BASE=http://localhost:8000
VITE_WS_BASE=ws://localhost:8000
